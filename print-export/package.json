{
  "name": "lim2026-flyer-print-export",
  "version": "1.0.0",
  "description": "Render print-ready PDFs from LIM flyers and documents at exact letter size using headless Chromium. Every printable page is exported here rather than relying on a visitor\u2019s print dialog.",
  "type": "module",
  "private": true,
  "scripts": {
    "export": "node export-flyer.js"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
